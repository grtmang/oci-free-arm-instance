name: Try to Create OCI VM

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

env:
  # 这里全面读取你在 Step 4 中设置的 11 个 Secrets
  COMPARTMENT_ID: ${{ secrets.OCI_COMPARTMENT_ID }}
  IMAGE_ID: ${{ secrets.IMAGE_ID }}
  SUBNET_ID: ${{ secrets.OCI_SUBNET_ID }}
  AD_NAME: ${{ secrets.AD_NAME }}
  OCI_CLI_REGION: ${{ secrets.OCI_CLI_REGION }}

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Add OCI CLI to path
        run: echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Setup OCI CLI Config
        run: |
          pip install oci-cli
          mkdir -p ~/.oci
          
          # 安全写入私钥
          cat << 'EOF' > ~/.oci/oci_api_key.pem
          ${{ secrets.OCI_CLI_KEY_CONTENT }}
          EOF
          chmod 600 ~/.oci/oci_api_key.pem

          # 动态写入所有的认证配置
          echo "[DEFAULT]" > ~/.oci/config
          echo "user=${{ secrets.OCI_CLI_USER }}" >> ~/.oci/config
          echo "tenancy=${{ secrets.OCI_CLI_TENANCY }}" >> ~/.oci/config
          echo "fingerprint=${{ secrets.OCI_CLI_FINGERPRINT }}" >> ~/.oci/config
          echo "key_file=~/.oci/oci_api_key.pem" >> ~/.oci/config
          echo "region=${{ env.OCI_CLI_REGION }}" >> ~/.oci/config
          
          # 修复权限警告
          chmod 600 ~/.oci/config
          
          echo "${{ secrets.SSH_PUBLIC_KEY }}" > /home/runner/my_ssh_key.pub

      - name: 🔍 测试 API 认证
        run: |
          echo "正在向甲骨文服务器验证你的 API 凭证..."
          oci iam region list --output table || { echo "❌ 认证失败！请重新检查 Secrets 里的 User、Tenancy、Fingerprint 和 API_KEY"; exit 1; }
          echo "✅ 认证成功！开始排队抢机器..."

      - name: Try to Launch VM
        id: create_vm
        continue-on-error: true 
        run: |
          set -o pipefail
          OCI_OUTPUT=$(oci compute instance launch \
            --compartment-id "$COMPARTMENT_ID" \
            --availability-domain "$AD_NAME" \
            --shape "VM.Standard.A1.Flex" \
            --shape-config '{"ocpus":4,"memoryInGBs":24}' \
            --subnet-id "$SUBNET_ID" \
            --image-id "$IMAGE_ID" \
            --ssh-authorized-keys-file "/home/runner/my_ssh_key.pub" \
            --assign-public-ip true \
            --display-name "coolify-vm" \
            --boot-volume-size-in-gbs 100 2>&1 || true)
            
          echo "log_data<<EOF_LOG" >> $GITHUB_OUTPUT
          echo "$OCI_OUTPUT" >> $GITHUB_OUTPUT
          echo "EOF_LOG" >> $GITHUB_OUTPUT
          
          if echo "$OCI_OUTPUT" | grep -q '"lifecycle-state": "PROVISIONING"'; then
            echo "🎉 抢占成功！"
            exit 0
          elif echo "$OCI_OUTPUT" | grep -q 'Out of host capacity'; then
            echo "⏳ 缺货排队中，5分钟后将自动重试..."
            exit 1
          else
            echo "❌ 遇到其他错误"
            exit 1
          fi

      - name: Send Discord Notification
        if: always()
        uses: sarisia/actions-status-discord@v1.15.4
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          title: "OCI VM Status: ${{ steps.create_vm.outcome || 'failure' }}"
          status: ${{ steps.create_vm.outcome || 'failure' }}
          color: ${{ steps.create_vm.outcome == 'success' && '0x28a745' || '0xdc3545' }}
          description: |
            **Status:** `${{ steps.create_vm.outcome || 'failure' }}`
            **Log Output:**
            ```json
            ${{ steps.create_vm.outputs.log_data || '认证失败，跳过执行。请检查凭证！' }}
            ```
